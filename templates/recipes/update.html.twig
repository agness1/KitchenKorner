{% extends "./base.html.twig" %}

{% block body %}
<div class="">

    {{ form_start(form) }}

        {{ form_widget(form) }}
        
        <button    
            type="submit"
            >
            Submit
        </button>
    {{ form_end(form) }}
</div>
{% endblock %}